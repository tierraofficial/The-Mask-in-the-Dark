// =========================================
// 游戏核心配置 (Game Configuration)
// =========================================

export const GRID_SIZE = 5;       // 网格大小 (5x5)
export const TILE_SIZE = 120;     // 单个格子像素宽 (600px / 5)

// 视觉与动画
export const ANIMATION_SPEED = 0.04; // 动画渐变速度 (越小越慢，模拟老式LCD残影)

// =========================================
// 3D 渲染配置 (Raycaster Settings)
// =========================================
export const RENDER_3D_SETTINGS = {
    // 光照与雾效
    // 修改这些值可以调整场景的亮暗对比
    MIN_BRIGHTNESS: 0.3,    // 最暗亮度 (0.0 - 1.0), 即使完全黑暗时的最低可见度
    BRIGHT_FOG_DIST: 15.0,  // 明亮房间的视野距离/雾效起始距离 (越大看得越远)
    DARK_FOG_DIST: 4.0      // 黑暗房间的视野距离 (越小越压抑，视野越窄)
};

// 颜色定义 (Warm Retro Palette)
// -----------------------------------------------------
// 🎨 1. 液晶屏幕显示颜色 (LCD Screen / Canvas)
// 这些颜色仅影响屏幕内的游戏画面 (格子、光标等)
// -----------------------------------------------------
export const COLORS = {
    // --- 基础网格 ---
    BG: '#101010',        // 屏幕背景底色 (极深，接近黑色)
    OFF: '#2a2a2a',       // 熄灭的格子 (深灰)，代表黑暗区域
    ON: '#fdfdca',        // 点亮的格子 (暖白)，代表安全区域

    // --- UI & 实体 ---
    PLAYER: '#fffacd',    // 玩家光标 (亮柠檬黄)
    SPIRIT: '#ff4444',    // 恶灵 (复古红)
    HIGHLIGHT: 'rgba(255, 255, 200, 0.1)', // 鼠标悬停/交互高亮
    CORRUPT: '#ff0000'    // 破坏/腐蚀特效
};

// -----------------------------------------------------
// 🎨 2. 掌机外壳颜色 (Device Case)
// 请移步至 [style.css] 的 :root 变量中修改:
// --case-color: 外壳主色
// --case-shadow: 外壳阴影
// -----------------------------------------------------

// =========================================
// 游戏规则配置 (Game Rules)
// =========================================

export const GAME_SETTINGS = {
    // 玩家设置
    PLAYER_START: { x: 2, y: 2 }, // 玩家出生点 (中心)

    // 恶灵设置
    SPIRIT_HP: 3,                 // 恶灵生命值 (被光照几次后驱逐)
    TRAP_THRESHOLD: 5,            // 困兽阈值 (如果连通暗区小于此值，视为被困，触发破坏)

    // 回合设置
    TURN_TIME_LIMIT: 3000,        // 回合限时 (毫秒)

    // 控制设置
    MOUSE_SENSITIVITY: 0.001,     // 鼠标灵敏度 (原 0.002)
    MOVE_SPEED: 0.04,             // 玩家移动速度

    // 关卡生成
    LIGHT_CHANCE: 0.5,            // 初始亮灯概率
    SPAWN_ATTEMPTS: 100,          // 恶灵生成位置尝试次数 (防止死循环)

    // 行动点
    PLAYER_AP: 1,                 // 玩家每回合行动点数

    // 显形技能
    SPIRIT_REVEAL_LIMIT: 3,       // 每局使用次数限制
    SPIRIT_REVEAL_DURATION: 500,  // 显形持续时间 (毫秒)

    // 惩罚设置
    IDLE_PENALTY_LIGHTS: 3        // 待机惩罚熄灭灯及数量
};
